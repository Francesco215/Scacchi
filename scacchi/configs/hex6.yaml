# From-scratch Hex6 numerical-Q21 trial.

defaults:
  - hex
  - _self_

run:
  max_num_iters: 50

env:
  board_size: 6


selfplay:
  max_num_steps: 36

eval:
  checkpoint_path: checkpoints/6_solved

checkpointing:
  max_to_keep: 0
  save_interval_steps: 10
  directory: checkpoints/hex6_aznet_dirichlet_c128_l6_seed0
