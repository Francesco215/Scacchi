# Effective board/run-specific overrides for W&B run pal/scacchi-az/cl3xe7ka
# ("hex_9_dt_solved"). The shared algorithmic recipe lives in hex.yaml.

defaults:
  - hex
  - _self_

run:
  max_num_iters: 300

env:
  board_size: 9

model:
  num_channels: 128
  num_layers: 6

selfplay:
  batch_size: 4096
  max_num_steps: 81

eval:
  checkpoint_path: checkpoints/9_solved

checkpointing:
  max_to_keep: 0
  save_interval_steps: 5
  directory: checkpoints/asd
