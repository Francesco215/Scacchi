# From-scratch Hex8 convolutional numerical-Q21 trial.

defaults:
  - hex
  - _self_

run:
  max_num_iters: 200

env:
  board_size: 8


selfplay:
  max_num_steps: 64

eval:
  checkpoint_path: checkpoints/8_solved

checkpointing:
  max_to_keep: 0
  save_interval_steps: 10
  directory: checkpoints/asd
