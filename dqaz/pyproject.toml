[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "dqaz"
version = "0.1.0"
requires-python = "==3.11.*"
dependencies = ["numpy"]

[tool.maturin]
module-name = "dqaz._dqaz"
python-source = "python"
features = ["pyo3/extension-module"]
